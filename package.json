{
  "name": "@tuck/www-dantuck",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "dev:search": "astro build && pagefind --site dist && astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "publish-preview": "astro build && pagefind --site dist && wrangler pages publish dist --project-name=www-dantuck --branch=preview",
    "publish-release": "astro build && pagefind --site dist && wrangler pages publish dist --project-name=www-dantuck",
    "screenshots": "node scripts/capture-screenshots.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  },
  "devDependencies": {
    "@astrojs/mdx": "^5.0.0",
    "@astrojs/sitemap": "^3.7.1",
    "@astrojs/svelte": "^8.0.0",
    "@unocss/astro": "^66.6.6",
    "astro": "^6.0.4",
    "pagefind": "^1.1.0",
    "playwright": "^1.58.2",
    "satori": "^0.25.0",
    "sharp": "^0.34.5",
    "svelte": "^5.53.11",
    "unocss": "^66.6.6",
    "wrangler": "^4.73.0"
  }
}
