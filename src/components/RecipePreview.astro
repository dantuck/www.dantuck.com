---
import Tags from './Tags.astro';

export interface Props {
  post: any;
  hero?: boolean;
}
const { post, hero = false } = Astro.props;
const { publishDate, title, prepTime, cookTime, tags, description } = post.frontmatter;
const isoDate = publishDate ? new Date(publishDate).toISOString().slice(0, 10) : undefined;
---

{hero ? (
  <div class="hero-card">
    <a href={post.url} class="card-link">
      <h2 class="hero-title">{title}</h2>
      <div class="hero-meta">
        {prepTime && <span class="time-badge">Prep: {prepTime}</span>}
        {cookTime && <span class="time-badge">Cook: {cookTime}</span>}
        <time datetime={isoDate} class="hero-date">{publishDate}</time>
      </div>
      {description && <p class="hero-description">{description}</p>}
    </a>
    {tags?.length > 0 && <Tags {tags} />}
  </div>
) : (
  <div class="post-card">
    <a href={post.url} class="card-link">
      <h2 class="card-title">{title}</h2>
      <div class="card-meta">
        {prepTime && <span class="time-label">Prep: {prepTime}</span>}
        {cookTime && <span class="time-label">Cook: {cookTime}</span>}
        <time datetime={isoDate} class="card-date">{publishDate}</time>
      </div>
      {description && <p class="card-description">{description}</p>}
    </a>
    {tags?.length > 0 && <Tags {tags} />}
  </div>
)}

<style>
  .hero-card {
    background: var(--color-bg-raised);
    border-radius: 6px;
    border: 1px solid var(--color-border);
    border-left: 3px solid var(--color-accent);
    padding: 2rem 2.25rem;
    transition: border-color 0.15s ease;
    margin-bottom: 1.5rem;
  }

  .hero-card:hover {
    border-color: var(--color-accent);
  }

  .card-link {
    display: block;
    text-decoration: none;
  }

  .hero-title {
    color: var(--color-heading);
    font-family: "Fraunces Variable", Georgia, Cambria, "Times New Roman", serif;
    font-weight: 700;
    font-size: 1.8rem;
    line-height: 1.2;
    margin: 0 0 0.6rem;
  }

  .hero-meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .time-badge {
    background: var(--color-bg-raised);
    border: 1px solid var(--color-border);
    color: var(--color-accent);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
  }

  .hero-date {
    color: var(--color-text-muted);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin-left: auto;
  }

  .hero-description {
    color: var(--color-text);
    font-size: 0.95rem;
    line-height: 1.7;
    margin: 0 0 0.75rem;
  }

  .post-card {
    background: var(--color-bg-raised);
    border-radius: 6px;
    border: 1px solid var(--color-border);
    padding: 1.5rem 1.75rem;
    transition: border-color 0.15s ease;
  }

  .post-card:hover {
    border-color: var(--color-accent);
  }

  .card-title {
    color: var(--color-heading);
    font-size: 1rem;
    line-height: 1.3;
    margin: 0 0 0.25rem;
  }

  .card-meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
    margin-bottom: 0.5rem;
  }

  .time-label {
    color: var(--color-accent);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .card-date {
    color: var(--color-text-muted);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin-left: auto;
  }

  .card-description {
    color: var(--color-text);
    font-size: 0.85rem;
    line-height: 1.6;
    margin: 0 0 0.5rem;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
